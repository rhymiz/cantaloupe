name: "Google Login"
steps:
  - action: go
    input: https://www.google.com
  - action: click
    selector: "blablal"
  - action: code
    input: |
      if (new RegExp("/*.store/").test(page.url)) {
        alert("has");
      }
  - action: screenshot
    input:
      options:
        timeout: 1
    template: |
      await page.screenshot({{input.options | safe}})