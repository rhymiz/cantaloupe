name: "Google Login"
steps:
  - action: go
    input: https://www.google.com
  - action: click
    selector: "blablal"
  - action: code
    input: |
      if (/*.store/.match(page.url)) {
        alert("has");
      }
  - action: screenshot
    input:
      options:
        timeout: 1
    template: |
      await page.screenshot({{input.options | safe}})