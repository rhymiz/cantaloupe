{%- if input -%}
await page.locator('{{selector.ref}}'{% if selector.options %},{{ selector.options }}{% endif %}).{{event}}('{{input}}',{{input.options}});
{%- else -%}
await page.locator('{{selector.ref}}'{% if selector.options %},{{ selector.options }}{% endif %}).{{event}}({{input.options}});
{%- endif %}
