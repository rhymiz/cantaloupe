{%- if input -%}
await page.locator('{{selector.ref}}',{{ selector.options }}).{{event}}('{{input}}',{{input.options}})
{%- else -%}
await page.locator('{{selector.ref}}',{{ selector.options }}).{{event}}({{input.options}})
{%- endif %}
