{%- if input.ref -%}
await page.{{event}}('{{input.ref}}'{% if input.options %}, {{input.options}}{% endif %});
{%- else -%}
await page.{{event}}('{{input}}'{% if input.options %}, {{input.options}}{% endif %});
{%- endif -%}